import { createContext } from 'react';

export const IngredientsContext = createContext(null);
export const SelectedIngredientsContext = createContext(null);
export const ReceivedDataContext = createContext(null);
export const TotalPriceContext = createContext(null);
